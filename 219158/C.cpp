#include<iostream>

using namespace std;

int main()
{
  int x, y;
  cin >> x >> y;

  cin >> a >> b;
  // scanf("%d %d", &x, &y);
  
  // cout << c << endl; // 0.75
  
  cout << x << " + " << y << " = "  << x + y << endl; 
  cout << x << " * " << y << " = " <<  (long long)x * y << endl;
  cout << x << " - " << y << " = " << x - y << endl;
  
  return 0;
}
